// Parte 1
function getRandomInt(max) {
  return Math.floor(Math.random() * max);
}
console.log(getRandomInt(2));

// Parte 2
let pi = Math.PI;
let piRedondeado = Math.round(pi);
console.log("Redondeo seria:",piRedondeado);
